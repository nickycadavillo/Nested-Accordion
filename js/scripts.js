// trying to open first row

// function openAcc() {
// var heading = document.getElementsByClassName('heading-big')
//
// var paragraph = document.getElementsByClassName('default')
  // if (paragraph.hasAttribute('default')) {
  //    paragraph.removeAttribute('default');
//   }
// };

var heading = document.querySelector('h2');
var paragraph = document.getElementsByClassName('default');

heading.addEventListener('click', function() {
  alert("Hi");
  // document.getElementsByClassName('default').className = "";
  // if (paragraph.className === "default"){
  //     paragraph.className"show";
  //   }
  //   else {
  //     paragraph.className === "default"
  //     }
  // if (paragraph.classList.contains(default)){
  //   paragraph.classList.remove(default);
  //   paragraph.classList.add(show);
  // if (paragraph.classList === "default"){
  //   paragraph.classList === "show";
  // }
  // else {
  //   paragraph.classList === "default"
});
